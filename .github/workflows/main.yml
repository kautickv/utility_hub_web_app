on:
  push:
    branches:
      - main
jobs:
  execute-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Build React Front-end
        run: |
          cd ./front_end
          npm build
          echo "Build complete"
          ls